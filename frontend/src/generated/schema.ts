// GENERATED by scripts/sync-schema.mjs - do not edit.
//
// The canonical SQLite schema, extracted from internal/storage/schema.go
// and the ADD COLUMN migrations in internal/storage/storage.go. Run
// `npm run sync:schema` after changing either.
//
// Regenerating rather than hand-copying is deliberate: the copy that used
// to live in teslamateImport.ts drifted, losing the whole geocode_cache
// table and two positions columns, which produced imported databases that
// silently failed the Locations dashboard.

/** CREATE TABLE / CREATE INDEX statements, in dependency order. */
export const schemaStatements: readonly string[] = [
  `CREATE TABLE IF NOT EXISTS schema_meta (
key TEXT PRIMARY KEY,
value TEXT NOT NULL
)`,
  `CREATE TABLE IF NOT EXISTS vehicles (
id INTEGER PRIMARY KEY AUTOINCREMENT,
vin TEXT NOT NULL UNIQUE,
tesla_id TEXT,
display_name TEXT,
model TEXT,
trim_badging TEXT,
marketing_name TEXT,
exterior_color TEXT,
wheel_type TEXT,
spoiler_type TEXT,
efficiency_wh_km REAL,
firmware_version TEXT,
created_at TEXT NOT NULL DEFAULT (strftime('%Y-%m-%dT%H:%M:%fZ','now'))
)`,
  `CREATE TABLE IF NOT EXISTS states (
id INTEGER PRIMARY KEY AUTOINCREMENT,
vehicle_id INTEGER NOT NULL REFERENCES vehicles(id),
state TEXT NOT NULL,
started_at TEXT NOT NULL,
ended_at TEXT
)`,
  `CREATE INDEX IF NOT EXISTS idx_states_vehicle_started ON states(vehicle_id, started_at)`,
  `CREATE INDEX IF NOT EXISTS idx_states_open ON states(vehicle_id, ended_at)`,
  `CREATE TABLE IF NOT EXISTS drives (
id INTEGER PRIMARY KEY AUTOINCREMENT,
vehicle_id INTEGER NOT NULL REFERENCES vehicles(id),
start_time TEXT NOT NULL,
end_time TEXT,
start_odometer_km REAL,
end_odometer_km REAL,
distance_km REAL,
duration_min REAL,
start_battery_level INTEGER,
end_battery_level INTEGER,
start_range_km REAL,
end_range_km REAL,
start_ideal_range_km REAL,
end_ideal_range_km REAL,
start_lat REAL,
start_lng REAL,
end_lat REAL,
end_lng REAL,
start_location TEXT,
end_location TEXT,
max_speed_kmh REAL,
max_power_kw REAL,
min_power_kw REAL,
outside_temp_avg_c REAL,
inside_temp_avg_c REAL,
ascent_m REAL,
descent_m REAL,
status TEXT NOT NULL DEFAULT 'open'
)`,
  `CREATE INDEX IF NOT EXISTS idx_drives_vehicle_start ON drives(vehicle_id, start_time)`,
  `CREATE INDEX IF NOT EXISTS idx_drives_status ON drives(vehicle_id, status)`,
  `CREATE TABLE IF NOT EXISTS positions (
id INTEGER PRIMARY KEY AUTOINCREMENT,
drive_id INTEGER NOT NULL REFERENCES drives(id),
vehicle_id INTEGER NOT NULL REFERENCES vehicles(id),
timestamp TEXT NOT NULL,
latitude REAL,
longitude REAL,
speed_kmh REAL,
heading REAL,
elevation_m REAL,
power_kw REAL,
odometer_km REAL,
battery_level INTEGER,
usable_battery_level INTEGER,
range_km REAL, -- rated
ideal_range_km REAL,
est_range_km REAL,
battery_heater_on INTEGER,
battery_heater INTEGER,
battery_heater_no_power INTEGER,
outside_temp_c REAL,
inside_temp_c REAL,
fan_status INTEGER,
driver_temp_setting_c REAL,
passenger_temp_setting_c REAL,
is_climate_on INTEGER,
is_rear_defroster_on INTEGER,
is_front_defroster_on INTEGER,
tpms_pressure_fl REAL,
tpms_pressure_fr REAL,
tpms_pressure_rl REAL,
tpms_pressure_rr REAL,
shift_state TEXT,
sentry_mode INTEGER,
is_user_present INTEGER,
valet_mode INTEGER,
climate_keeper_mode TEXT
)`,
  `CREATE INDEX IF NOT EXISTS idx_positions_drive ON positions(drive_id, timestamp)`,
  `CREATE TABLE IF NOT EXISTS charging_sessions (
id INTEGER PRIMARY KEY AUTOINCREMENT,
vehicle_id INTEGER NOT NULL REFERENCES vehicles(id),
start_time TEXT NOT NULL,
end_time TEXT,
start_battery_level INTEGER,
end_battery_level INTEGER,
start_range_km REAL,
end_range_km REAL,
start_ideal_range_km REAL,
end_ideal_range_km REAL,
charge_energy_added_kwh REAL,
charge_energy_used_kwh REAL,
max_charger_power_kw REAL,
outside_temp_avg_c REAL,
cost REAL,
latitude REAL,
longitude REAL,
location TEXT,
is_dc_fast_charge INTEGER,
status TEXT NOT NULL DEFAULT 'open'
)`,
  `CREATE INDEX IF NOT EXISTS idx_charging_sessions_vehicle_start ON charging_sessions(vehicle_id, start_time)`,
  `CREATE INDEX IF NOT EXISTS idx_charging_sessions_status ON charging_sessions(vehicle_id, status)`,
  `CREATE TABLE IF NOT EXISTS charging_samples (
id INTEGER PRIMARY KEY AUTOINCREMENT,
charging_session_id INTEGER NOT NULL REFERENCES charging_sessions(id),
vehicle_id INTEGER NOT NULL REFERENCES vehicles(id),
timestamp TEXT NOT NULL,
battery_level INTEGER,
usable_battery_level INTEGER,
charger_power_kw REAL,
charger_voltage REAL,
charger_actual_current REAL,
charger_pilot_current INTEGER,
charger_phases INTEGER,
conn_charge_cable TEXT,
fast_charger_present INTEGER,
fast_charger_brand TEXT,
fast_charger_type TEXT,
charge_energy_added_kwh REAL,
range_km REAL, -- rated
ideal_range_km REAL,
battery_heater_on INTEGER,
not_enough_power_to_heat INTEGER,
outside_temp_c REAL,
charge_limit_soc INTEGER
)`,
  `CREATE INDEX IF NOT EXISTS idx_charging_samples_session ON charging_samples(charging_session_id, timestamp)`,
  `CREATE TABLE IF NOT EXISTS battery_samples (
id INTEGER PRIMARY KEY AUTOINCREMENT,
vehicle_id INTEGER NOT NULL REFERENCES vehicles(id),
timestamp TEXT NOT NULL,
battery_level INTEGER,
battery_range_km REAL,
ideal_battery_range_km REAL,
source TEXT NOT NULL
)`,
  `CREATE INDEX IF NOT EXISTS idx_battery_samples_vehicle_ts ON battery_samples(vehicle_id, timestamp)`,
  `CREATE TABLE IF NOT EXISTS geocode_cache (
lat_key REAL NOT NULL,
lng_key REAL NOT NULL,
name TEXT NOT NULL,
road TEXT,
city TEXT,
county TEXT,
state TEXT,
postcode TEXT,
country TEXT,
created_at TEXT NOT NULL DEFAULT (strftime('%Y-%m-%dT%H:%M:%fZ','now')),
PRIMARY KEY (lat_key, lng_key)
)`,
  `CREATE TABLE IF NOT EXISTS software_updates (
id INTEGER PRIMARY KEY AUTOINCREMENT,
vehicle_id INTEGER NOT NULL REFERENCES vehicles(id),
version TEXT NOT NULL,
status TEXT NOT NULL,
start_time TEXT NOT NULL,
end_time TEXT
)`,
  `CREATE INDEX IF NOT EXISTS idx_software_updates_vehicle ON software_updates(vehicle_id, start_time)`,
]

/**
 * ADD COLUMN migrations for columns introduced after a table's first
 * release. Applied after schemaStatements; each may already exist, which
 * is not an error.
 */
export const columnMigrations: readonly string[] = [
  'ALTER TABLE drives ADD COLUMN start_location TEXT',
  'ALTER TABLE drives ADD COLUMN end_location TEXT',
  'ALTER TABLE charging_sessions ADD COLUMN location TEXT',
  'ALTER TABLE positions ADD COLUMN sentry_mode INTEGER',
  'ALTER TABLE positions ADD COLUMN is_user_present INTEGER',
  'ALTER TABLE positions ADD COLUMN valet_mode INTEGER',
  'ALTER TABLE positions ADD COLUMN climate_keeper_mode TEXT',
  'ALTER TABLE charging_samples ADD COLUMN charge_limit_soc INTEGER',
  'ALTER TABLE charging_sessions ADD COLUMN is_dc_fast_charge INTEGER',
  'ALTER TABLE vehicles ADD COLUMN firmware_version TEXT',
  'ALTER TABLE positions ADD COLUMN battery_heater INTEGER',
  'ALTER TABLE positions ADD COLUMN battery_heater_no_power INTEGER',
  'ALTER TABLE geocode_cache ADD COLUMN road TEXT',
  'ALTER TABLE geocode_cache ADD COLUMN city TEXT',
  'ALTER TABLE geocode_cache ADD COLUMN county TEXT',
  'ALTER TABLE geocode_cache ADD COLUMN state TEXT',
  'ALTER TABLE geocode_cache ADD COLUMN postcode TEXT',
  'ALTER TABLE geocode_cache ADD COLUMN country TEXT',
]

/** Every table the schema defines. */
export const schemaTables: readonly string[] = [
  'schema_meta',
  'vehicles',
  'states',
  'drives',
  'positions',
  'charging_sessions',
  'charging_samples',
  'battery_samples',
  'geocode_cache',
  'software_updates',
]
